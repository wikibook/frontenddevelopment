if(!naver){
    /**
     * @namespace
     */
    var naver = {};
}

/**
 * @namespace
 */
naver.model = naver.model || {};

/**
 * @namespace
 */
naver.collection = naver.collection || {};

/**
 * @namespace
 */
naver.view = naver.view || {};
